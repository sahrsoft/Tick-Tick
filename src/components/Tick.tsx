import { IonContent } from '@ionic/react'
import React from 'react'

const Tick: React.FC = () => {

    return (
        <IonContent className="ion-padding">
            Tick
        </IonContent>
    )
}

export default Tick